# Configuration file for the database connection settings
import os
# from dotenv import load_dotenv

# Load environment variables from the .env file
# load_dotenv()
# Read database configuration from environment variables or define them directly

DATABASE_URL = "postgresql+asyncpg://postgres:admin123@localhost/rtodatabase"

